{
  "name": "primeprop-nigeria",
  "version": "1.0.0",
  "description": "PrimeProp Nigeria — Property listings platform on Cloudflare Workers",
  "private": true,
  "scripts": {
    "deploy": "cd worker && npx wrangler deploy",
    "dev": "cd worker && npx wrangler dev",
    "migrate": "cd worker && npx wrangler d1 migrations apply primeprop-db --remote",
    "typecheck": "cd worker && npx tsc --noEmit"
  }
}
